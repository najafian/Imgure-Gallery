import React from 'react';
import { Switch } from 'react-router-dom';


const Routes = ({ match }) => (
  <div>
    <Switch>
      {/* prettier-ignore */}
    </Switch>
  </div>
);

export default Routes;
